name: N1满血全家桶+手机全套 每月自动更新
on:
  workflow_dispatch:
  schedule:
    - cron: '0 18 1 * *'
env:
  TZ: Asia/Shanghai
  UPLOAD_RELEASE: true
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v5

      - name: 安装编译依赖
        run: |
          sudo apt update -y
          sudo apt install -y build-essential flex bison gawk libncurses-dev git libssl-dev unzip libelf-dev libltdl-dev libpcap-dev libcap-dev libaio-dev libbz2-dev zlib1g-dev libusb-1.0-0-dev libev-dev libnl-3-dev libnl-genl-3-dev golang rustc cargo nodejs npm wget curl

      - name: 初始化源码
        run: |
          git clone -b openwrt-24.10 https://github.com/immortalwrt/immortalwrt openwrt
          cd openwrt
          git config --global user.name "bot"
          git config --global user.email "bot@github.com"

      - name: 检测上游更新
        id: check_update
        run: |
          cd openwrt
          git fetch origin openwrt-24.10
          LOCAL=$(git rev-parse HEAD)
          REMOTE=$(git rev-parse origin/openwrt-24.10)
          if [ "$LOCAL" = "$REMOTE" ];then
            echo "IS_UPDATE=false" >> $GITHUB_ENV
            echo "无更新跳过编译"
          else
            echo "IS_UPDATE=true" >> $GITHUB_ENV
            git pull origin openwrt-24.10
          fi

      - name: 添加插件源
        if: env.IS_UPDATE == 'true'
        run: |
          cd openwrt
          sed -i '$a src-git kenzo https://github.com/kenzok8/openwrt-packages' feeds.conf.default
          sed -i '$a src-git small https://github.com/kenzok8/small' feeds.conf.default
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: N1架构内核分区
        if: env.IS_UPDATE == 'true'
        run: |
          cd openwrt
          make defconfig
          sed -i '/CONFIG_TARGET_/d' .config
          echo "CONFIG_TARGET_armsr=y" >> .config
          echo "CONFIG_TARGET_armsr_armv8=y" >> .config
          echo "CONFIG_LINUX_6_6=y" >> .config
          echo "CONFIG_TARGET_KERNEL_PARTSIZE=512" >> .config
          echo "CONFIG_TARGET_ROOTFS_PARTSIZE=8192" >> .config
          echo "CONFIG_TARGET_IMAGES_GZIP=y" >> .config
          echo "CONFIG_TARGET_EXT4=y" >> .config
          echo "CONFIG_TARGET_SQUASHFS=y" >> .config
          echo "CONFIG_TARGET_KVM=y" >> .config
          echo "CONFIG_TARGET_VIRTUALIZATION=y" >> .config
          make defconfig

      - name: 全家桶+手机全套配置
        if: env.IS_UPDATE == 'true'
        run: |
          cd openwrt
          # 系统美化商店
          echo "CONFIG_PACKAGE_luci=y" >> .config
          echo "CONFIG_PACKAGE_luci-i18n-base-zh-cn=y" >> .config
          echo "CONFIG_PACKAGE_luci-theme-argon=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-argon-config=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-store=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-iStore=y" >> .config
          # 科学上网全套
          echo "CONFIG_PACKAGE_luci-app-passwall=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-openclash=y" >> .config
          # 多拨分流加速
          echo "CONFIG_PACKAGE_luci-app-mwan3=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-turboacc=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-flowoffload=y" >> .config
          # 网盘共享
          echo "CONFIG_PACKAGE_luci-app-alist=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-samba4=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-rclone=y" >> .config
          # 容器虚拟机
          echo "CONFIG_PACKAGE_luci-app-dockerman=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-macvlan=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-kvm=y" >> .config
          # 去广告DNS
          echo "CONFIG_PACKAGE_luci-app-adguardhome=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-smartdns=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-adblock=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-hosts=y" >> .config
          # 内网穿透DDNS
          echo "CONFIG_PACKAGE_luci-app-frpc=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-zerotier=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-wireguard=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-ddns=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-cloudflare=y" >> .config
          # 硬件监控
          echo "CONFIG_PACKAGE_htop=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-diskman=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-wrtbwmon=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-speedtest=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-cpufreq=y" >> .config
          # 游戏多开防封
          echo "CONFIG_PACKAGE_luci-app-nft-qos=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-vlan-switch=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-macbind=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-splitrouting=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-gameaccelerate=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-gamespeed=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-multiaccount=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-isolateclient=y" >> .config
          # 智能家居
          echo "CONFIG_PACKAGE_luci-app-homeassistant=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-miio=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-tuya=y" >> .config
          echo "CONFIG_PACKAGE_kmod-bluetooth=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-bluetooth=y" >> .config
          # 影音IPTV下载
          echo "CONFIG_PACKAGE_luci-app-iptv=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-jellyfin=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-aria2=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-qbittorrent=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-transmission=y" >> .config
          # 校园网WiFi认证
          echo "CONFIG_PACKAGE_luci-app-mentohust=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-h3c=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-wifidog=y" >> .config
          # 手机全套+备份全套
          echo "CONFIG_PACKAGE_luci-app-backup=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-cloudsync=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-syncthing=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-filetransfer=y" >> .config
          echo "CONFIG_PACKAGE_usb-modeswitch=y" >> .config
          echo "CONFIG_PACKAGE_kmod-usb-storage=y" >> .config
          echo "CONFIG_PACKAGE_kmod-usb-net=y" >> .config
          echo "CONFIG_PACKAGE_kmod-usb-net-rndis=y" >> .config
          echo "CONFIG_PACKAGE_kmod-usb-net-cdc-ether=y" >> .config
          echo "CONFIG_PACKAGE_mount-utils=y" >> .config
          echo "CONFIG_PACKAGE_rsync=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-androidbackup=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-iphonemanage=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-usbshare=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-phonesync=y" >> .config
          make defconfig

      - name: 编译固件
        if: env.IS_UPDATE == 'true'
        run: |
          cd openwrt
          make -j$(nproc) V=s 2>&1 | tee build.log

      - name: 上传固件
        if: env.IS_UPDATE == 'true'
        uses: actions/upload-artifact@v5
        with:
          name: N1满血全家桶手机全套固件
          path: openwrt/bin/targets/armsr/armv8/*

      - name: 发布固件
        if: env.IS_UPDATE == 'true'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: N1-Full-Phone-Backup
          name: N1满血全家桶+手机全套备份固件
          body: 每月1号北京时间凌晨2点自动检测更新，全插件全家桶，内置手机USB共享+整机备份
          files: openwrt/bin/targets/armsr/armv8/*
