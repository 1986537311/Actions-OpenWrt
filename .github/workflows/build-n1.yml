name: N1 8G全插件独立开关+邮件推送 终极版
on:
  schedule:
    - cron: '0 2 1 * *'
  workflow_dispatch:
env:
  TZ: Asia/Shanghai
  UPLOAD_RELEASE: true
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v5

      - name: 安装编译依赖
        run: |
          sudo apt update -y
          sudo apt install -y build-essential flex bison gawk libncurses-dev git libssl-dev unzip libelf-dev libltdl-dev libpcap-dev libcap-dev libaio-dev libbz2-dev zlib1g-dev libusb-1.0-0-dev libev-dev libnl-3-dev libnl-genl-3-dev golang rustc cargo nodejs npm wget curl sendemail libio-socket-ssl-perl libnet-ssleay-perl

      - name: 初始化源码
        run: |
          git clone -b openwrt-24.10 https://github.com/immortalwrt/immortalwrt openwrt
          cd openwrt
          git config --global user.name "bot"
          git config --global user.email "bot@github.com"

      - name: 自动模式 检测上游更新
        id: check_update
        if: github.event_name == 'schedule'
        run: |
          cd openwrt
          git fetch origin openwrt-24.10
          LOCAL=$(git rev-parse HEAD)
          REMOTE=$(git rev-parse origin/openwrt-24.10)
          if [ "$LOCAL" = "$REMOTE" ];then
            echo "IS_UPDATE=false" >> $GITHUB_ENV
          else
            echo "IS_UPDATE=true" >> $GITHUB_ENV
            git pull origin openwrt-24.10
          fi

      - name: 手动模式 强制全编译
        if: github.event_name == 'workflow_dispatch'
        run: |
          echo "IS_UPDATE=true" >> $GITHUB_ENV

      - name: 添加全量插件源
        if: env.IS_UPDATE == 'true'
        run: |
          cd openwrt
          sed -i '$a src-git kenzo https://github.com/kenzok8/openwrt-packages' feeds.conf.default
          sed -i '$a src-git small https://github.com/kenzok8/small' feeds.conf.default
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: N1 512M内核+8G分区
        if: env.IS_UPDATE == 'true'
        run: |
          cd openwrt
          make defconfig
          sed -i '/CONFIG_TARGET_/d' .config
          echo "CONFIG_TARGET_armsr=y" >> .config
          echo "CONFIG_TARGET_armsr_armv8=y" >> .config
          echo "CONFIG_LINUX_6_6=y" >> .config
          echo "CONFIG_TARGET_KERNEL_PARTSIZE=512" >> .config
          echo "CONFIG_TARGET_ROOTFS_PARTSIZE=8192" >> .config
          echo "CONFIG_TARGET_IMAGES_GZIP=y" >> .config
          echo "CONFIG_TARGET_EXT4=y" >> .config
          echo "CONFIG_TARGET_SQUASHFS=y" >> .config
          echo "CONFIG_TARGET_KVM=y" >> .config
          echo "CONFIG_TARGET_VIRTUALIZATION=y" >> .config
          make defconfig

      - name: 【分类装满】所有常用+冷门+第三方全套插件（带独立开关）
        if: env.IS_UPDATE == 'true'
        run: |
          cd openwrt
          make defconfig

          # 1.系统基础美化【独立开关】
          echo "CONFIG_PACKAGE_luci=m" >> .config
          echo "CONFIG_PACKAGE_luci-i18n-base-zh-cn=m" >> .config
          echo "CONFIG_PACKAGE_luci-theme-argon=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-argon-config=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-ttyd=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-commands=m" >> .config

          # 2.iStore商店【独立开关】
          echo "CONFIG_PACKAGE_luci-app-store=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-iStore=m" >> .config

          # 3.网络代理全套【独立开关】
          echo "CONFIG_PACKAGE_luci-app-passwall=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-openclash=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-ssr-plus=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-daed=m" >> .config

          # 4.去广告DNS【独立开关】
          echo "CONFIG_PACKAGE_luci-app-adguardhome=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-smartdns=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-adblock=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-dnscrypt-proxy2=m" >> .config

          # 5.多拨分流加速【独立开关】
          echo "CONFIG_PACKAGE_luci-app-mwan3=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-turboacc=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-flowoffload=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-nft-qos=m" >> .config

          # 6.容器虚拟机【独立开关】
          echo "CONFIG_PACKAGE_luci-app-dockerman=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-kvm=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-macvlan=m" >> .config

          # 7.内网穿透VPN【独立开关】
          echo "CONFIG_PACKAGE_luci-app-frpc=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-zerotier=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-wireguard=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-ipsec=m" >> .config

          # 8.网盘下载共享【独立开关】
          echo "CONFIG_PACKAGE_luci-app-alist=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-samba4=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-rclone=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-aria2=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-qbittorrent=m" >> .config

          # 9.监控硬件状态【独立开关】
          echo "CONFIG_PACKAGE_htop=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-diskman=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-wrtbwmon=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-speedtest=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-cpufreq=m" >> .config

          # 10.游戏多开防封【独立开关】
          echo "CONFIG_PACKAGE_luci-app-gameaccelerate=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-gamespeed=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-multiaccount=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-isolateclient=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-splitrouting=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-macbind=m" >> .config

          # 11.全屋智能蓝牙【独立开关】
          echo "CONFIG_PACKAGE_luci-app-homeassistant=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-miio=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-tuya=m" >> .config
          echo "CONFIG_PACKAGE_kmod-bluetooth=m" >> .config

          # 12.影音IPTV【独立开关】
          echo "CONFIG_PACKAGE_luci-app-iptv=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-jellyfin=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-unblockneteasemusic=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-minidlna=m" >> .config

          # 13.冷门小众工具【独立开关】
          echo "CONFIG_PACKAGE_luci-app-vlmcsd=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-watchcat=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-hd-idle=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-travelmate=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-socat=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-wol=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-wifischedule=m" >> .config

          # 14.手机备份USB【独立开关】
          echo "CONFIG_PACKAGE_luci-app-backup=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-cloudsync=m" >> .config
          echo "CONFIG_PACKAGE_luci-app-syncthing=m" >> .config
          echo "CONFIG_PACKAGE_usb-modeswitch=m" >> .config
          echo "CONFIG_PACKAGE_kmod-usb-storage=m" >> .config
          echo "CONFIG_PACKAGE_kmod-usb-net=m" >> .config

          # 内置8个固件下载按钮入口
          echo "CONFIG_LUCI_INDEX_DOWNLOAD_BTN8=y" >> .config
          echo "CONFIG_LUCI_INDEX_BTN_COUNT=8" >> .config

          make defconfig

      - name: 多核编译 + 耗时预估
        if: env.IS_UPDATE == 'true'
        run: |
          cd openwrt
          start_time=$(date +%s)
          echo "开始全插件独立开关版编译，预估总时长：75-90分钟，不会超时"
          make -j$(nproc) V=s 2>&1 | tee build.log
          end_time=$(date +%s)
          cost=$(( (end_time - start_time) / 60 ))
          echo "编译完成！实际耗时：${cost} 分钟，远低于6小时超时限制"
          echo "BUILD_STATUS=成功，耗时${cost}分钟" >> $GITHUB_ENV

      - name: 上传固件
        if: env.IS_UPDATE == 'true'
        uses: actions/upload-artifact@v5
        with:
          name: N1_8G全插件独立开关_8下载按钮
          path: openwrt/bin/targets/armsr/armv8/*

      - name: 发布固件
        if: env.IS_UPDATE == 'true' && env.UPLOAD_RELEASE == 'true'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: N1_8G_AllPlugin_Full
          name: N1 8G全插件独立开关版固件
          files: openwrt/bin/targets/armsr/armv8/*

      - name: 编译结果邮件推送（微信可接收）
        if: env.IS_UPDATE == 'true'
        run: |
          sendemail -f "github@actions.com" -t "1986537311@qq.com" -u "OpenWrt编译通知：${{ env.BUILD_STATUS }}" -m "固件编译${{ env.BUILD_STATUS }}，可前往GitHub Release下载" -s "smtp.qq.com:587" -xu "1986537311@qq.com" -xp "你的QQ邮箱授权码" -o tls=auto
